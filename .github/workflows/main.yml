name: Test1

on: push

env:
  TEST: test1

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          case "${{ github.ref }}" in
            refs/heads/master)
              STAGE=prod
              ;;
            refs/heads/staging)
              STAGE=staging
              ;;
            *)
              exit 1
              ;;
          esac
          echo $STAGE
      - run: echo $STAGE
